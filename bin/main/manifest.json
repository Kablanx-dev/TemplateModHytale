{
    "Group": "${group}",
    "Name": "${name}",
    "Version": "${version}",
    "Description": "${description}",
    "Authors": [
        {
            "Name": "kablanxdev"
        }
    ],
    "ServerVersion": "${server_version}",
    "Dependencies": {},
    "OptionalDependencies": {},
    "DisabledByDefault": false,
    "Main": "${entry_point}"
}
